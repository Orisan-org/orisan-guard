# Orisan Guard Agent Instructions

Guard is separate from Orisan Scout.

Do not add Scout repo-scanning behavior here. Do not add Scout MCP or agent-instruction scanners here.

Do not add browser extension, cloud sync, control plane, network calls, or LLM calls unless a task explicitly asks for that scope.

Keep Guard Alpha focused on local sensitive-context classification, placeholder planning, redaction, evidence-safe reporting, and synthetic benchmark coverage.
